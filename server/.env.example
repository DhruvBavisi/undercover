# MongoDB Connection (Required)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/undercover-game?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Client URL for CORS
CLIENT_URL=http://localhost:5173

# For production deployment (uncomment and update when deploying)
# NODE_ENV=production
# CLIENT_URL=https://your-production-client-url
